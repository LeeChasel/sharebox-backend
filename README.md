# sharebox-backend

## How to start

1. Install dependencies:
    ```bash
    bun install
    ```

2. Set up environment variable

    ```bash
    cp .env.example .env
    ```
    and set the values


3. Run

    ```bash
    bun run dev
    ```
